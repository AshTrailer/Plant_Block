现在需要一个新的模块，这个是最核心的浇水程序。这个程序经由两个土壤湿度传感器双通道采样触发。这里先做这个12V的卡默尔的蠕动泵的触发代码。要求给我这个起一个新的英文名字。时间通过time_manager模块进行采集。4小时内只能触发一次，单次触发31.5s，也就是40ml(测得线性拟合程度99.98%，1.068ml/s),31.5s这个为可变变量。然后按周算次数，这个周次数也为可变变量，也就是比如一周有着最小浇水次数和最大浇水次数。比如说，假如一周最小1次，最大5次，那这周假如触发了6次，那么第六次就不会触发。同理，假如一周一次都没有触发。那么过了这周就会触发一次，但是不计入下一周的计数。还有触发阈值，这里使用百分数，也就是0-100，比如我取50作为触发阈值，那么大约等于50就会触发。周的概念从时间模块获取，每周一00:00为一周的起始，然后周日的23：59：59为这周的结束。我要求触发函数是单独的函数，方便改写。然后开始编写测试函数，首先我要求该测试函数调用input_parser，能输入格式为mositure + xx，这里xx为二位的数字，我通过输入值来测试是否触发。你要保留接口，方便后续接入command processor获取泵是否启动的数据，还有当前阈值，浇水次数和已经浇水次数，单次浇水时长。这里泵是通过mosfet进行触发的，因此只需要对应pin进行高电平就行。这里设置GPIO口为GPIO 15。还有个就是这里不需要实时检测，因为后续土壤湿度传感器会在检测前两秒才上电，检测之后两秒就断电，也就是中间若是时刻检测，可能会检测出非合理的值。这里在每四个小时判断是否需要浇水前需要提前一段时间发送信号启动土壤湿度传感器。